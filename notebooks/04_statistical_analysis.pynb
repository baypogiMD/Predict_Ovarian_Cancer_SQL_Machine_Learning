# Statistical Comparison: Benign vs Malignant

from scipy.stats import mannwhitneyu

cancer = df[df["type"] == 0]
benign = df[df["type"] == 1]

stat, p = mannwhitneyu(cancer["ca125"], benign["ca125"])
stat, p

# Effect size (Cohen's d)
def cohens_d(x, y):
    return (x.mean() - y.mean()) / ((x.std() + y.std()) / 2)

cohens_d(cancer["ca125"], benign["ca125"])
